# V1.44 — BUILD RECOVERY / AUDIT

## Objetivo
Parar a expansão funcional e preparar o primeiro ciclo de verificação de compilação real.

## Alterações
- Versão Android: 1.44 / versionCode 51.
- Removida a implementação duplicada e sem referências `com.nexus.ai.agent.TaskController`.
- Mantida a implementação usada por `MainActivity`: `com.nexus.ai.TaskController`.
- CI passa a gerar um Gradle Wrapper 8.9 durante o job e executar `./gradlew test` e `./gradlew :app:assembleDebug`.
- O CI usa Java 17, alinhado ao ambiente suportado pelo Android Gradle Plugin configurado.

## Resultado atual
- Auditoria estática concluída.
- Compilação local: NÃO EXECUTADA neste ambiente porque não há Gradle instalado e o projeto ainda não possui wrapper versionado.
- Testes unitários: NÃO EXECUTADOS neste ambiente.
- APK: NÃO GERADO neste ambiente.

## Gateway model
A configuração `gpt-5.6-luna` NÃO deve ser tratada como modelo fictício sem verificação. A documentação atual da OpenAI lista `gpt-5.6-luna` como Model ID. Portanto, não foi alterada nesta etapa.

## Próximo gate
Executar o workflow/ambiente com Gradle real. Registrar cada erro de compilação e corrigir somente problemas reproduzidos.
