# NEXUS V1.34 — Validação

## Objetivo
Corrigir a identidade da build e fechar corretamente o ciclo iniciado pela UI, inclusive quando uma autorização de rede é negada.

## Alterações
- `app/build.gradle.kts`: versionCode 42 / versionName 1.34.
- `MainActivity.kt`: ciclo atual mantido na UI; eventos de ciclo recebem `cycleId`; bloqueio de rede fecha o ciclo como `BLOCKED`; diagnóstico prioriza o ciclo atual.
- `README.md`: marco V1.34 e próximo marco V1.35 documentados.

## Verificações estáticas
- [x] Nenhuma referência de versão de interface permanece em V1.33.
- [x] `CYCLE_BEGIN` registra `cycleId`.
- [x] `CYCLE_COMPLETE` registra `cycleId`.
- [x] `CYCLE_FAILED` registra `cycleId`.
- [x] `CYCLE_BLOCKED` registra `cycleId`.
- [x] Caminho de rede negada finaliza o trace.
- [x] `currentCycleId` é limpo no `finally`.

## Limitação
Este ambiente não possui Gradle Wrapper nem comando `gradle` disponível; portanto o `test` e o `assembleDebug` ainda precisam ser executados no CI/Android Studio.

## Próximo marco
V1.35 — executar a validação real de build/CI e, depois, teste em aparelho físico.
